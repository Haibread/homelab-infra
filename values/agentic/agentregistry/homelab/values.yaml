config:
  jwtPrivateKey: "66CwpCJY1cZ7pFazFONDOWV9eJ2SesSJSgiGlLiWiPw=" # Switch to a secret later

database:
  postgres:
    # Dummy value to satisfy chart validation — overridden at runtime by extraEnvVars below
    url: "postgres://placeholder"
    bundled:
      enabled: false

extraEnvVars:
  - name: AGENT_REGISTRY_DATABASE_URL
    valueFrom:
      secretKeyRef:
        name: agentregistry-pg-app
        key: uri
