serviceMonitor:
  enabled: true

sources:
  - ingress
  - service
  - gateway-httproute
  - gateway-tlsroute
  - gateway-tcproute
  - gateway-udproute

policy: sync

env:
  - name: ETCD_URLS
    value: https://etcd.external-dns:2379
  - name: ETCD_CA_FILE
    value: /etc/ssl/certs/etcd-client-tls/ca.crt
  - name: ETCD_CERT_FILE
    value: /etc/ssl/certs/etcd-client-tls/tls.crt
  - name: ETCD_KEY_FILE
    value: /etc/ssl/certs/etcd-client-tls/tls.key

extraVolumes:
  - name: etcd-client-tls
    secret:
      secretName: etcd-client-tls

extraVolumeMounts:
  - name: etcd-client-tls
    mountPath: /etc/ssl/certs/etcd-client-tls
    readOnly: true

domainFilters:
  - newgamer.lan

provider:
  name: coredns

rbac:
  create: true

resources:
  requests:
    cpu: 100m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 128Mi