apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana
  namespace: argocd
spec:
  project: default
  sources:
  - repoURL: ghcr.io/grafana-community/helm-charts
    targetRevision: "12.3.3"
    chart: grafana
    helm:
      valueFiles:
      - $values/values/monitoring/grafana/base/values.yaml
      - $values/values/monitoring/grafana/homelab/values.yaml
  - repoURL: https://github.com/Haibread/homelab-infra.git
    targetRevision: "main"
    ref: values
  - repoURL: https://github.com/Haibread/homelab-infra.git
    targetRevision: "main"
    path: manifests/monitoring/grafana
  destination:
    name: in-cluster
    namespace: monitoring
  ignoreDifferences:
  - kind: Secret
    name: grafana
    namespace: monitoring
    jsonPointers:
    - /data/admin-password
  - group: apps
    kind: Deployment
    name: grafana
    namespace: monitoring
    jsonPointers:
    - /spec/template/metadata/annotations/checksum~1secret
  syncPolicy:
    managedNamespaceMetadata:
      labels:
        pod-security.kubernetes.io/enforce: privileged
    #automated:
    #  selfHeal: true
    #  prune: true
    syncOptions:
    - ApplyOutOfSyncOnly=true
    - CreateNamespace=true
    - IncludeMutationWebhook=true
    - RespectIgnoreDifferences=true
    - ServerSideApply=true
    - ServerSideDiff=true
    - Validate=false
